name: apt-hunt-browser-policy
version: 1.2.0
description: Enforces bounded sequential browser Hunts and removes API-backed web search.
provides_tools:
  - web_search
hooks:
  - pre_tool_call
  - transform_tool_result
  - on_session_end
capabilities:
  - tools.override
